"""Plans package — survey plan rendering helpers.

Submodules are imported explicitly where needed (e.g.
``from plans.aspose_renderer import render_plan_to_vsdx``); this package
initialiser intentionally imports nothing so importing one submodule never
drags in the others.
"""
